<?xml version="1.0" encoding="UTF-8"?>

<Configuration>

  <Properties>

    <Property name="APP_LOG_LEVEL">${bundle:application:app.logging.level}</Property>
    <Property name="LOG_FILE_PATH">${bundle:application:log.file.path}</Property>
    <Property name="LOG_FILE_NAME">${bundle:application:log.file.name}</Property>

    <Property name="CONSOLE_LOG_PATTERN">
      %d{${LOG_DATEFORMAT_PATTERN:-yyyy-MM-dd HH:mm:ss.SSS}} %highlight{${LOG_LEVEL_PATTERN:-%5p}}{FATAL=bg_red, ERROR=red, WARN=yellow, INFO=green, DEBUG=blue} %style{${sys:PID}}{magenta} [%15.15t] %style{%-40.40C{1.}}{cyan} : %m%n${LOG_EXCEPTION_CONVERSION_WORD:-%wEx}
    </Property>

    <Property name="FILE_LOG_PATTERN">
      %d %p %c{1.} [%t] %m%n
    </Property>

  </Properties>

  <Appenders>

    <Console name="ConsoleAppender" target="SYSTEM_OUT" follow="true">
      <PatternLayout pattern="${CONSOLE_LOG_PATTERN}"/>
      <!-- <Filters> -->
      <!--   <BurstFilter level="INFO" rate="10" maxBurst="100"/> -->
      <!-- </Filters> -->
    </Console>

    <RollingFile name="FileAppender" fileName="${LOG_FILE_PATH}/${LOG_FILE_NAME}" filePattern="${LOG_FILE_PATH}/$${date:yyyy-MM}/app-%d{MM-dd-yyyy}-%i.log.gz">
      <!-- <PatternLayout pattern="${FILE_LOG_PATTERN}"/> -->
      <JSONLayout complete="true" compact="false"/>
      <Policies>
        <OnStartupTriggeringPolicy />
        <SizeBasedTriggeringPolicy size="50 MB" />
      </Policies>
    </RollingFile>

    <Sentry name="Sentry" />

  </Appenders>

  <Loggers>
    
    <Logger name="AppLogger" level="DEBUG" additivity="false">
      <AppenderRef ref="ConsoleAppender"/>
      <AppenderRef ref="FileAppender"/>
       <appender-ref ref="Sentry" level="ERROR" />
    </Logger>

    <!-- everything not logged by the AppLogger  -->
    <Root level="INFO">
      <AppenderRef ref="ConsoleAppender" level="WARN"/>
      <AppenderRef ref="FileAppender" level="WARN"/>
      <!-- Sentry logging threshold is overridden to the ERROR level -->
      <appender-ref ref="Sentry" level="ERROR" />
    </Root>

  </Loggers>
</Configuration>
